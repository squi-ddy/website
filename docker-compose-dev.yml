version: "3.8"

services:
  backend:
    build: ./backend
    command: "npm start"
    env_file:
      - ./backend/.env
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    command: "npm start"
    ports:
      - "3001:8000"