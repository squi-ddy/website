version: "3.8"

services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    restart: on-failure
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    restart: on-failure
    ports:
      - "3001:8000"